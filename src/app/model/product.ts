export class Product {
    
    serial:number;
    name:string;
    quantity:number;

    constructor(serial:number, name:string, quantity:number){
        this.serial=serial;
        this.name=name;
        this.quantity=quantity;
    }
}
